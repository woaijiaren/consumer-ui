import { ArticleTitleDirective } from './article-title.directive';

describe('ArticleTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticleTitleDirective();
    expect(directive).toBeTruthy();
  });
});
