import { WorkerTypePipe } from './worker-type.pipe';

describe('WorkerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
