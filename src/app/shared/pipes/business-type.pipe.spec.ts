import { BusinessTypePipe } from './business-type.pipe';

describe('BusinessTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BusinessTypePipe();
    expect(pipe).toBeTruthy();
  });
});
