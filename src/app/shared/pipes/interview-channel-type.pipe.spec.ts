import { InterviewChannelTypePipe } from './interview-channel-type.pipe';

describe('InterviewChannelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InterviewChannelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
